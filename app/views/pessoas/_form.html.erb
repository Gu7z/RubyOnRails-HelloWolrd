<%= form_with(model: pessoa, local: true) do |form| %>
  <% if pessoa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pessoa.errors.count, "error") %> prohibited this pessoa from being saved:</h2>

      <ul>
      <% pessoa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pessoa_name %>
  </div>

  <div class="field">
    <%= form.label :idade %>
    <%= form.text_field :idade, id: :pessoa_idade %>
  </div>

  <div class="field">
    <%= form.label :altura %>
    <%= form.text_field :altura, id: :pessoa_altura %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
